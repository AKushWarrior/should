part of 'should_statement.dart';

/*
class Transition extends BaseShouldObject {
  Transition(_obj) : super(_obj);

  @override
  void noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);
}
*/

// Available in should.js, may be useful in the future.
